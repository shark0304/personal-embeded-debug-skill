interface:
  display_name: "Embedded Debug Workbench"
  short_description: "Evidence-first embedded failure workflow"
  brand_color: "#0F766E"
  default_prompt: "Use $embedded-debug to onboard or triage this embedded project, or mine public embedded repos: create project memory, score readiness, collect packets, suggest evidence, match patterns, review reports, and track failure cases."

policy:
  allow_implicit_invocation: true
