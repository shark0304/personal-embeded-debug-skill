interface:
  display_name: "Embedded Debug Workbench"
  short_description: "Evidence-first project triage for embedded firmware debug"
  brand_color: "#0F766E"
  default_prompt: "Use $embedded-debug to detect my embedded project context, collect a debug packet, score evidence completeness, and produce a verification-ready triage report."

policy:
  allow_implicit_invocation: true
